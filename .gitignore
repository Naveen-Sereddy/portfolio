# Dependencies
node_modules/

# OS / editor junk
.DS_Store
Thumbs.db
*.log

# Local tooling / preview config (not needed in the repo)
.claude/

# My private notes (stay on my machine, not on GitHub)
PROJECT-CONTEXT.md

# Keep dist/styles.css committed so Vercel serves it with no build step.

# staging for case-study source (never ship)
_incoming:*/
_incoming/
